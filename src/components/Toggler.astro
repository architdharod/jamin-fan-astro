---
---

<button id='navbar-toggle' class='navbar-toggle' type='button'
  aria-label='Toggle navigation menu' aria-expanded='false' aria-controls='sidebar-nav'
>
  <svg id="icon-list" xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" fill="currentColor" viewBox="0 0 16 16" aria-hidden="true">
    <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5m0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5m0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5"/>
  </svg>
  <svg id="icon-close" xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" fill="currentColor" viewBox="0 0 16 16" aria-hidden="true" style="display:none;">
    <path d="M2.146 2.854a.5.5 0 1 1 .708-.708L8 7.293l5.146-5.147a.5.5 0 0 1 .708.708L8.707 8l5.147 5.146a.5.5 0 0 1-.708.708L8 8.707l-5.146 5.147a.5.5 0 0 1-.708-.708L7.293 8z"/>
  </svg>
</button>

<script type='module'>
  const navbarToggle = document.querySelector('#navbar-toggle');
  const iconList = document.querySelector('#icon-list');
  const iconClose = document.querySelector('#icon-close');
  const header = document.querySelector('header');

  function openNav() {
    header.style.display = 'block';
    navbarToggle.setAttribute('aria-expanded', 'true');
    iconList.style.display = 'none';
    iconClose.style.display = '';

    // Move focus to the first focusable element inside the nav
    const firstFocusable = header.querySelector('a, button, [tabindex]:not([tabindex="-1"])');
    if (firstFocusable) firstFocusable.focus();

    // Add focus trap and escape listener
    document.addEventListener('keydown', handleNavKeydown);
  }

  function closeNav() {
    header.style.display = 'none';
    navbarToggle.setAttribute('aria-expanded', 'false');
    iconList.style.display = '';
    iconClose.style.display = 'none';

    // Return focus to the toggle button
    navbarToggle.focus();

    document.removeEventListener('keydown', handleNavKeydown);
  }

  function handleNavKeydown(e) {
    if (e.key === 'Escape') {
      closeNav();
      return;
    }

    // Focus trap: keep Tab cycling within the header + toggle button
    if (e.key === 'Tab') {
      const focusableEls = [
        ...header.querySelectorAll('a[href], button, [tabindex]:not([tabindex="-1"])'),
        navbarToggle,
      ];
      if (focusableEls.length === 0) return;

      const firstEl = focusableEls[0];
      const lastEl = focusableEls[focusableEls.length - 1];

      if (e.shiftKey) {
        if (document.activeElement === firstEl) {
          e.preventDefault();
          lastEl.focus();
        }
      } else {
        if (document.activeElement === lastEl) {
          e.preventDefault();
          firstEl.focus();
        }
      }
    }
  }

  navbarToggle.addEventListener('click', () => {
    const display = getComputedStyle(header).display;
    if (display === 'none') {
      openNav();
    } else {
      closeNav();
    }
  });
</script>

<style>
  #navbar-toggle {
    position: fixed;
    bottom: 1em;
    right: 1em;
    z-index: 9999;

    font-size: 1.8rem;
    width: 1.5em;
    aspect-ratio: 1;
    border: 0;
    border-radius: 50%;
    background: transparent;
    color: #f5d9ce;
    mix-blend-mode: difference;

    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
  }
</style>
